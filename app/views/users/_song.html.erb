<div id="<%= song.id%>" class="col-xs">
	<div id="songCard" class="card text-center bg-faded">
		<div class="card-block">
			<h4 class="card-title has-popover" data-toggle="popover" data-content="<%= song.title%>">
				<%= truncate(song.title.html_safe, length: 12)%></h4></a>
			<h6 class="card-subtitle mb-2"><%= song.album%></h6>
			<p class="card-text text-muted"><i>by <%= song.artist%> in <%= song.year%></i></p>
			<%= link_to "Play on YouTube", show_video_path(song), class: 'btn btn-secondary',
				method: :get, remote: true %>
		</div>
		<!-- <div class="card-footer">
			<div id="edit_music" style="margin: auto;text-align: center">
				<%#= link_to 'Edit', {controller: 'musics',action: 'edit',id: song.id,token: 'music'}, 
					method: :get, class: 'btn btn-info btn-sm', remote: true %>&nbsp;&nbsp;
				<a href="<%= delete_confirm_music_path(song)%>" data-remote="true"
					data-params="id=<%= song.id%>" class="btn btn-danger btn-sm">Delete</a>	
			</div>
		</div> -->
	</div>
</div>